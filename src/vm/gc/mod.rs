// first arc as gc
// when the heap is full
    // allocate new space
    // do tri color gc
    // check avaliable ratio
    // free the memory
pub mod heap;
pub mod rc;
mod test_rc;
// pub mod tri_color;
mod test_heap;
// TODO: prob a arc on a tri color could be faster approch
trait Heap{
    
}